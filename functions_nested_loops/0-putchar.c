#include <stdio.h>

/**
 * main - print a text based on n
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char text[] = "_putchar";
	for (a = 0; a < strlen(text); a++)
	{
		putchar('text[i]')
	}

	putchar('\n')

	return (0);
}
