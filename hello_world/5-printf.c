#include <stdio.h>

/**
 * main - Print text
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char text[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", text);

	return (0);
}
