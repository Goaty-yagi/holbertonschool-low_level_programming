#include "main.h"

/**
 * print_numbers - calls print_numbers
 *
 * Return: check if parameter is lower case
 */

void print_numbers(void)
{
	int n;

	n = 0;

	while (0 < 9)
	{
		_putchar('0' + n);

		n = n + 1;
	}
	_putchar('\n');
}
