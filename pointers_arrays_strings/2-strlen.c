#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _strlen - calls _strlen
 * @n: char array
 *
 * Return: length of string
 */

int _strlen(char *s)
{
    return strlen(s); // from string.h
}